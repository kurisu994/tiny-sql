# Changelog

本文件记录 tiny-sql 的版本变化，面向使用者说明主要功能和体验变化，不展开内部实现细节。

格式基于 [Keep a Changelog](https://keepachangelog.com/)，遵循[语义化版本](https://semver.org/)。

---

## [Unreleased]

### 新功能

- 新增 PostgreSQL 连接类型，可浏览当前数据库下的 Schema、表和列，并执行、限流及取消 SQL；连接表单会自动带出 PostgreSQL 常用端口和用户默认值。
- SSH 拓扑按跳显示低频协议 RTT 采样和超时状态；该数值明确标注为累计到对应 SSH session 的探测往返时间，不冒充 ICMP 或单段链路延迟。
- 数据库树支持按需展开表列，直接查看列类型、是否可空、索引标记、默认值和注释。
- SQL 编辑器按连接使用 MySQL/PostgreSQL 方言，可补全已加载的列和表别名，并根据 `user_id → users.id` 等实际列关系提供可直接应用的 JOIN 片段。
- 新增主密码保护（可选启用）：连接配置改用 Argon2id 派生密钥加密，支持启动解锁、手动锁定、错误密码拒绝和忘记密码重置；旧加密配置在首次启用时自动无损迁移，迁移失败可原样恢复。
- 启用主密码后可在输入 SSH 私钥 passphrase 时选择「记住」，加密保存后重启应用免输入。
- 新增 SQL 历史：最近 100 条执行记录（含连接、库/Schema、时间和成功状态）加密落盘，点击回填当前查询，支持一键清空。
- 查询工作台支持多 tab：每个 tab 独立保存 SQL、结果集和取消状态，可同时执行多条查询互不干扰；关闭未执行或执行中的 tab 会先确认。
- 查询结果可导出 CSV / Excel：在后端重新执行并流式写入所选文件，区分 SQL NULL 与空字符串，大结果集不会复制到前端序列化。
- 结果表格列宽可拖拽调整，按连接和结果结构记忆，可随时恢复默认。
- MySQL SSL 页支持文件选择器填写 CA / 客户端证书 / 私钥路径；TLS 握手失败与证书校验失败分别给出可操作的错误提示。

### 体验优化

- 从应用菜单手动检查更新且没有新版本时，弹窗提示“当前已是最新版本”；检查失败时也会显示明确结果。
- 连接列表明确标识 MySQL / PostgreSQL，PostgreSQL 按“数据库 → Schema → 表”层级展示，避免混淆两种数据库的命名空间语义。
- Schema 元数据使用按连接和命名空间隔离的内存 LRU cache；支持手动刷新，并在重连、建库或执行 DDL 后自动失效。
- 快速来回切换 database、Schema 或表列时，过期异步响应不再回写当前对象树或 SQL 补全数据。
- SSH 隧道断开后可从顶部、断链提示或连接右键菜单一键重连；重连会先取消该连接的旧查询并清理旧连接池和隧道，迟到结果或旧 session 事件不会污染新会话。
- 测试连接支持带口令的 SSH 私钥；输入的 passphrase 只用于本次握手，不会保存。
- SSH 隧道运行中断开时会区分首跳掉线、嵌套通道关闭和内部监听异常，并避免正常关闭或重复事件造成误报。
- SSH keepalive 间隔与连续失败阈值可在高级设置中配置，新建连接默认启用 `60 秒 / 3 次`；关闭后不会再发送心跳。
- PostgreSQL 浏览非当前数据库时，错误横幅提供「一键切换」按钮：以会话级覆盖重建连接并展开该库的 Schema，不修改保存的连接配置，关闭后重新打开仍是原数据库。
- MySQL 语法错误可安全标出服务端行号；应用只接收错误 key 与行号，不会暴露原始数据库错误或 SQL 片段。
- 查询结果表格的 `#` 序号列冻结在左侧，横向滚动宽结果集时行号始终可见；表头继续吸顶，行列定位不迷失。

### 已知限制

- PostgreSQL 同一连接只能展开当前数据库；浏览其他数据库可通过错误横幅一键切换（仅本次会话生效，不改保存的连接配置）。证书路径配置尚未接入 PostgreSQL，当前使用驱动默认 TLS 策略。
- MySQL TLS 的错误提示已细化，真实 TLS 服务器（含双向证书）正反例已验收通过。
- SQL 历史在启用主密码但未解锁时不可读写；未启用主密码时由本机密钥加密保护。
- 导出 Excel 大结果集受查询 10 万行硬上限约束；超出请补充 `LIMIT` 分批导出。

---

## [0.1.0] — 2026-08-18

### 新功能

- SQL 编辑器支持执行 `SHOW` / `EXPLAIN` / `DESC` 等元数据语句并展示结果集，不再误判为写操作。

### 修复

- 修复点击 / 双击同一张表会开出多个同名预览 tab 的问题；已打开该表时点击会切换到已有 tab。
- 修复 `SELECT *` 多表 JOIN 因重复列名报错（1060 Duplicate column name）的问题；带 `LIMIT`、`FOR UPDATE` 等子句的查询不再被改写。
- 超出行数上限截断结果时主动终止服务端查询，避免大结果集在多跳隧道上继续传输。
- 修复并发查询占满连接池时相互等待导致整体卡死的问题。

### 安全与稳定性

- 「测试连接」现在同样校验 SSH 主机指纹（首次信任确认、指纹变更硬拒绝），不再接受任意主机密钥。

### 已知限制

- 带口令的 SSH 私钥暂不能通过「测试连接」输入 passphrase；保存连接后通过正式连接流程输入即可。
- SSH 运行期断开统一显示为对应跳板 `lost`，暂不细分 channel 主动关闭与本地 accept loop 异常。
- MySQL 服务端 SQL 错误暂不定位到编辑器具体行号，当前只显示稳定的中文错误提示。

---

## [0.0.3] — 2026-07-03

### 体验优化

- SQL 编辑区接入语法高亮、行号、错误标识和快捷执行，输入与排错体验更接近桌面数据库客户端。
- 数据库 / 表树新增图标并去掉数据库名前的展开箭头，提升多库、多表浏览时的识别度。

### 修复

- 修复数据库展开 / 收起交互：双击数据库才打开或切换，单击仅折叠 / 展开已打开的数据库；收起时保留当前表和查询结果，只有切换数据库时才重置表选择。

---

## [0.0.2] — 2026-07-01

### 新功能

- 已连接的连接右键菜单新增「新建数据库」，支持选择字符集、排序规则并预览 SQL。

### 体验优化

- 新建 / 编辑连接弹窗改为标签页布局，按常规、SSH、SSL 和高级设置分组。
- SSL 模式与证书路径可随连接保存；高级设置新增保持连接、连接 / 读取 / 写入超时、压缩和自动连接选项。
- 检查更新入口移到 macOS 应用菜单，连接列表头部不再放置更新按钮。

---

## [0.0.1] — 2026-06-30

### 新功能

- 初始桌面客户端能力：支持创建、编辑、复制、删除、测试并打开 MySQL 连接。
- 支持通过多跳 SSH 跳板连接内网 MySQL，并展示连接过程和断开状态。
- 支持首次连接新 SSH 主机时确认指纹，后续连接自动复用已信任记录。
- 支持浏览数据库、表、字段和表数据，并在内置 SQL 编辑器中执行查询。
- 支持取消长时间运行的 SQL，并在执行写操作前进行二次确认。
- 支持连接配置加密保存，避免敏感连接信息明文落盘。
- 支持应用内检查正式版更新、查看版本说明、下载更新并在安装后重启。

### 体验优化

- 连接列表改为更接近桌面客户端的操作方式：单击选中、双击连接、右键菜单管理连接。
- 新建 / 编辑连接、删除确认、SQL 写操作确认统一改为弹窗交互。
- 优化数据结果表格，较大结果集滚动更顺滑，并在结果被截断时给出提示。
- 优化连接拓扑展示，清楚呈现本机、跳板机和 MySQL 的连接路径与状态。
- 更新 tiny-sql 专属应用图标和左上角品牌标识。

### 安全与稳定性

- 增加 SQL 执行护栏，降低误执行多语句、空语句或危险写操作的风险。
- 增加 SSH keepalive 和连接丢失提示，方便定位是哪一跳出现问题。
- 指纹变更会被拒绝，避免连接到不可信的 SSH 主机。
- MySQL 连接默认更适配内网环境，减少因服务端 TLS 配置不一致导致的连接失败。

### 发布准备

- 完善 macOS、Windows、Linux 的桌面应用打包流程。
- 完善 GitHub Release 发布说明、预发布标记和正式版自动更新源生成。
- 修复正式版自动更新清单生成异常，确保 Linux 版本也能进入更新源。
- 补齐发布检查清单和 dogfooding 记录模板，为 v0.1 正式版试用做准备。

### 待验证

- 真实 3 跳 SSH + MySQL dogfooding 仍待完成。
- MySQL 5.7 兼容性仍待真实环境验证。
- 自动更新端到端流程仍需通过真实发布版本验证。
