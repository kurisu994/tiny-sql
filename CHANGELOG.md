# Changelog

本文件记录 tiny-sql 的版本变化，面向使用者说明主要功能和体验变化，不展开内部实现细节。

格式基于 [Keep a Changelog](https://keepachangelog.com/)，遵循[语义化版本](https://semver.org/)。

---

## [Unreleased]

### 体验优化

- 新建 / 编辑连接里点「测试连接」，成功后会显示延迟（握手完成后 `SELECT 1` 的往返），下面一行还列出 SSH 隧道建链、建立连接和总耗时，能一眼看出慢在哪一段。
- 工作台顶部的「浏览 / 关系图 / 对比 / 权限」和对象树操作（新建表 / 复制表 / 导入 SQL / 官方备份 / 刷新）合并成一栏，中间用竖线分隔，少占一行高度；对象树操作只在浏览视图显示。
- 连接列表可以拖动排序：按住列表项上下拖，松手即落位，顺序会保存下来，重启仍在。列表从此按你排的顺序显示，连接某个库后它不再自动跳到最上面；还没拖过的连接（含新建和导入的）排在后面，仍按最近使用时间倒序。

---

## [0.8.0] — 2026-08-29

### 新功能

- **新增 SQLite 支持**：新建连接可选 SQLite，选一个本地 `.db` 文件即可打开，不需要主机 / 账号。库树、表结构、数据浏览与编辑、SQL 编辑器（含 SQLite 方言补全与格式化）、CSV 导入、dump 导出与导入、结构对比与表复制都已接通；「应用只读」会让文件以只读方式打开。备份 / 恢复走官方 `sqlite3` 命令行。
- 连接可设「应用只读」：前端禁用写入口，后端拒绝写 SQL / 导入 / 恢复 / 拷贝目标 / ANALYZE。这不是数据库账号权限。
- 连接可打生产 / 预发 / 开发标签，列表圆点和确认框会带上。
- 选中表后可「复制表」：预览建表 SQL，手输新表名后创建，可选再拷数据。
- 结果格双击打开检查器，区分 NULL 与空串，JSON 会格式化；外键可跳到引用行。
- 修改表支持非主键列重命名（预览 `RENAME COLUMN`）。EXPLAIN 树会标出全表扫描 / 顺序扫描等提示。
- 应用菜单新增「Settings...」（⌘,），位于「Check for Updates...」下方：可开关自动检查更新、填更新代理、设置数据浏览默认每页行数、调 SQL 编辑器字号、开关写操作二次确认，并提供主密码管理入口与版本信息。改动即时生效，偏好存在本机，不随连接配置分享。
- 更新可走代理：设置里填好代理地址并点 ✓ 保存后，检查更新与下载更新包都经该代理；支持 `http` / `https` / `socks4` / `socks4a` / `socks5` / `socks5h`，留空为直连。点 ✕ 放弃本次改动。

### 体验优化

- **关系图首屏大幅提速**：结构改为一次性拉回整库（原先每张表要查列 / 索引 / 约束三轮），74 张表的库实测从约 10 秒降到 0.7 秒；切走再切回 5 分钟内直接用缓存，工具栏新增刷新按钮强制重拉。
- 关系图的缩放和平移改为直接绘制画布，不再逐帧走状态更新，触控板捏合 / 双指滚动不掉帧；实体卡片只在自身变化时重绘。
- **关系图重做为 ER 画布**：每张表按数据库实体展示——表头是表名与表注释，主键置顶，每列带键位图标（主键 / 外键 / 唯一 / 索引）、列注释和数据类型（`*` 表示 NOT NULL）；外键用鸦爪连线连到具体列，线走卡片外侧不再横穿。画布可拖动平移、滚轮平移、⌘/Ctrl + 滚轮缩放，工具栏提供放大 / 缩小 / 适应视图 / 复位，单张表也能拖着摆位。点表在对象树里选中它并高亮其关系，点连线看引用明细，点空白取消高亮。有外键的表按关系成组排在上方，孤立表在下方铺成网格；表多时可一键折叠成表头（超过 40 张表默认折叠）。
- 工作台「浏览 / 关系图 / 对比 / 权限」与对象树顶栏操作改为图标按钮，悬停仍显示原说明；对象树不再展示「数据库对象」标题。
- 设置 → 通用可切换外观：跟随系统 / 浅色 / 深色。默认跟随系统，立即生效并保存在本机；暗色底为 `#181818`。
- 会话页顶部链路图更紧凑，节点卡片宽度随内容自适应，IP+端口不再被截断成省略号（超长内容悬停可看全文）。
- 链路图延迟标签不再带「SSH」前缀，只显示毫秒 / 超时 / 不可用；进入数据库的边上会显示累计 `SELECT 1` 延迟（末跳不是 SSH session，测不到协议 RTT）。链路条改成可拖动画布，节点多了可以按住拖着看。

### 修复

- 连接成功后链路图横线上方的 SSH 延迟标签迟迟不显示：首个 RTT 采样事件会被连接流程的守卫或收尾重置吞掉，要等下一个采样周期才出现；现在连接收尾会保留已到达的采样，采样间隔也从 10 秒缩到 5 秒。
- 新建 / 编辑连接弹窗里点「测试连接」时，SSH 指纹信任（TOFU）弹窗不再被压在连接弹窗后面，可以正常点击。

### 已知限制

- SQLite：文件不存在时连接会失败（不自动建库）；没有账号与权限视图；`ALTER TABLE` 只支持改名 / 加列 / 删列，改类型、空性、默认值需重建表，修改表对话框会直接拦下；表定义里声明的 UNIQUE 约束（`sqlite_autoindex_*`）不能单独删除，同样需重建表；CHECK 约束不在结构页列出；复制表不带索引；官方备份（`sqlite3 .dump`）只能整库，单表请用内置 dump 导出。外键约束默认开启（SQLite 自身默认关闭）；改写数据库的 `PRAGMA`（如 `PRAGMA journal_mode = WAL`）按写操作处理，需二次确认。
- 「应用只读」不是数据库账号权限；未打开只读的连接仍可能误写。
- 设置里关掉「写操作二次确认」后，写语句不再弹确认直接执行；「应用只读」连接与后端护栏仍会拦截。
- 更新代理地址点 ✓ 时才校验并保存，非法地址会标红且不保存。代理仅作用于更新，数据库连接与 SSH 隧道不受影响。
- 复制表只在当前连接同库；MySQL `LIKE` 不含数据，PG DDL 为元数据重建。
- 外键跳转仅当前连接当前库；检查器不直接写回。
- 仍不能重命名或拆换主键。

---

## [0.7.0] — 2026-08-24

相对 [0.3.0] 的正式版。一次收录表格安全编辑、结构变更、官方备份、连接分享、双连接对比、表拷贝、MySQL 权限和 EXPLAIN 计划树。此前的 `0.4.0-rc1` 是预发布，不会作为自动更新源；本版本起重新进入正式版更新通道。

工作台可在「浏览 / 对比 / 关系图 / 权限」之间切换。对比与拷贝前请先打开两条连接。

### 新功能

- 表数据拷贝（FR-266）：对比台可将左连接表数据追加（或先清空再插入）到右连接同方言表；按列名映射，手输目标 `库.表` 才执行。
- MySQL 权限（FR-262）：工作台「权限」列出账号（不含密码哈希），可预览执行 CREATE USER / GRANT / REVOKE；PostgreSQL 仅只读角色。
- EXPLAIN（FR-222）：查询区「解释」展示计划树；「ANALYZE」会真正执行并需确认。
- 结构对比（FR-220）：工作台「对比」可对两条已打开连接比较表 / 列 / 索引 / 约束。切换连接焦点不再断开旧连接。跨数据库类型可以看差异，但不能一键同步。
- 结构同步脚本（FR-261）：按「把右侧改成与左侧一致」或相反方向生成可审阅 SQL，可保存或确认后在目标连接执行。不做数据拷贝。
- 只读关系图（FR-263）：工作台「关系图」用外键画出当前库的表关系，可筛选、缩放，点击表打开结构。
- 修改表与索引（FR-253）：结构页可改列（增/删/改类型/空性/默认值）并实时预览 ALTER，确认后执行；不能重命名列或拆换主键。可创建/删除非主键索引；外键与 CHECK 仅提供删除。视图在树中标记为 VIEW，结构只读。
- 官方备份 / 恢复（FR-260）：调用本机 mysqldump/mysql 或 pg_dump/pg_restore，经当前连接（含 SSH 隧道本地端口）。找不到官方工具会明确报错，不会改用「导出 SQL」。恢复前必须手输目标库名。
- 加密分享连接（FR-221）：用独立口令把选中连接导出为加密文件，同事用同一口令导入。默认不打包私钥文件；导入生成新连接、不覆盖已有项，也不带入对方主机指纹。
- 表格安全编辑（FR-250）：带主键表的浏览 tab 可进入「编辑」模式——双击单元格修改、行首删除、表尾新增行，全部变更先暂存前端（amber 高亮标记修改、红色划线标记删除、emerald 高亮标记新增），「提交」时在单个事务中批量执行，任一失败整体回滚并定位到第几条变更；「放弃」整体丢弃。翻页 / 筛选 / 排序 / 关闭 tab 时有未提交变更会先确认。无主键表不提供编辑入口；主键列不可修改；编辑单元格支持 Shift+Enter 置为 NULL（与空串严格区分）。
- 表结构查看与 DDL 预览（FR-251）：浏览 tab 新增「结构」视图，整合展示列定义（类型 / 可空 / 默认值 / 注释 / 主键标记）、索引与约束；建表 DDL 预览——MySQL 展示 `SHOW CREATE TABLE` 服务端原文，PostgreSQL 由元数据重建并明确标注非服务端原文。
- 新建表（FR-251）：schema 树顶「新建表」入口，结构化列编辑（类型下拉预选双方言常用类型并做白名单校验，主键 / 自增列强制 NOT NULL），实时生成双方言 `CREATE TABLE` SQL 预览，执行前确认对话框展示完整 SQL；成功后 schema 树自动刷新。
- CSV 导入（FR-252）：浏览 tab「导入 CSV」支持引号转义、内嵌换行、CRLF 与 UTF-8 BOM；列映射默认同名匹配可调整，空值语义与导出闭环（无引号 NULL 导入为 SQL NULL）；每批最多 1000 行，默认任一行失败整体停止并回滚该批，可选「跳过失败行」模式并在完成后报告失败行号。
- SQL dump 导入 / 导出（FR-252）：浏览 tab「导出 SQL」把当前表（或整库）的建表 DDL 与数据流式写为 SQL 文件；schema 树顶「导入 SQL」流式逐条执行大 SQL 文件（不整读内存），失败即中止并报告语句序号与位置预览。

### 修复

- MySQL 表结构页不再一直「加载结构…」：约束查询不再 JOIN 两张 information_schema 表（MySQL 8 无法下推过滤，会扫全实例），改为两条带库名/表名过滤的查询。

### 已知限制

- 修改表不支持重命名列或拆换主键；官方备份依赖本机已安装的 mysqldump/mysql 或 pg_dump/pg_restore。
- 结构对比切换焦点不会断开旧连接；跨 MySQL/PostgreSQL 只能看差异不能生成同步脚本；同步 DDL 失败通常无法整体回滚。
- 表格编辑仅支持带显式主键的单表；编辑提交期间他端改动同一行会报冲突并整体回滚（不做原值全比对乐观锁）。
- CSV 导入不做类型推断，值统一按文本由数据库隐式转换；导入大文件期间暂不显示百分比进度（可取消）。
- dump 导出的 PostgreSQL DDL 为简化重建（列 / 默认值 / 主键），不含索引、外键与 CHECK 约束；MySQL 为服务端原文。
- 表拷贝只支持已打开的同方言连接，按列名映射；跨 MySQL/PostgreSQL 不能拷。replace 会 TRUNCATE 目标表，必须手输 `库.表`。
- 权限页不展示密码哈希；PostgreSQL 只列出角色，变更请回 SQL 编辑器。
- EXPLAIN ANALYZE 会真正执行当前 SQL（含写语句副作用），必须单独确认。

---

## [0.3.0] — 2026-08-22

### 新功能

- 新增可靠事务（FR-244）：查询 tab 可「开始事务」，此后该 tab 所有 SQL 固定同一物理连接执行，随时「提交」或「回滚」，完成后自动回到普通模式；关闭 tab、断开或重连连接时未提交事务自动回滚，事务不随重连延续。PostgreSQL 事务内出错进入 aborted 状态时可直接一键回滚恢复。
- 表预览升级为服务端数据浏览（FR-242）：双击表打开的浏览 tab 支持按列筛选（= / ≠ / > / ≥ / < / ≤ / 包含 / 不包含 / 为空 / 非空）、点击列头排序和分页（100/500/1000 行每页），全部在服务端执行，不再一次拉取前 1000 行；总行数独立查询，超时自动降级为未知总数分页。
- schema 树展示索引与约束（FR-241）：展开表即可查看索引（名称、列、唯一性）与约束（主键 / 外键 / 唯一 / CHECK），外键直接标注引用目标；左侧新增对象搜索框，按名称即时过滤数据库、Schema、表、列、索引和约束，点击结果直接定位展开。
- SQL 编辑器支持执行多语句脚本（FR-243）：后端按方言拆分后逐条执行，每条独立护栏与写确认；一次执行保留全部结果集可切换查看，失败语句明确标注并中止后续语句（标记为已跳过）；事务控制语句在多语句脚本中会被拒绝并引导至事务 tab。新增「格式化」按钮，按当前连接方言美化 SQL。
- SQL 文件工作流（FR-240）：「文件」菜单支持打开 / 保存 / 另存为 SQL 文件（⌘O / ⌘S 快捷键），tab 标题关联文件名，保存自动清除未保存标记；文件被外部修改后保存会先提示覆盖确认；最近文件列表快速重开，失效文件自动清理。

### 修复

- 事务控制语句（`BEGIN` / `COMMIT` / `ROLLBACK` / `SAVEPOINT`）现在必须在事务 tab 中执行；普通 tab 执行会收到引导提示，不再于连接池连接上执行后污染后续查询。

---

## [0.2.0] — 2026-08-20

### 新功能

- 新增 PostgreSQL 连接类型，可浏览当前数据库下的 Schema、表和列，并执行、限流及取消 SQL；连接表单会自动带出 PostgreSQL 常用端口和用户默认值。
- SSH 拓扑按跳显示低频协议 RTT 采样和超时状态；该数值明确标注为累计到对应 SSH session 的探测往返时间，不冒充 ICMP 或单段链路延迟。
- 数据库树支持按需展开表列，直接查看列类型、是否可空、索引标记、默认值和注释。
- SQL 编辑器按连接使用 MySQL/PostgreSQL 方言，可补全已加载的列和表别名，并根据 `user_id → users.id` 等实际列关系提供可直接应用的 JOIN 片段。
- 新增主密码保护（可选启用）：连接配置改用 Argon2id 派生密钥加密，支持启动解锁、手动锁定、错误密码拒绝和忘记密码重置；旧加密配置在首次启用时自动无损迁移，迁移失败可原样恢复。
- 启用主密码后可在输入 SSH 私钥 passphrase 时选择「记住」，加密保存后重启应用免输入。
- 新增 SQL 历史：最近 100 条执行记录（含连接、库/Schema、时间和成功状态）加密落盘，点击回填当前查询，支持一键清空。
- 查询工作台支持多 tab：每个 tab 独立保存 SQL、结果集和取消状态，可同时执行多条查询互不干扰；关闭未执行或执行中的 tab 会先确认。
- 查询结果可导出 CSV / Excel：在后端重新执行并流式写入所选文件，区分 SQL NULL 与空字符串，大结果集不会复制到前端序列化。
- 结果表格列宽可拖拽调整，按连接和结果结构记忆，可随时恢复默认。
- MySQL SSL 页支持文件选择器填写 CA / 客户端证书 / 私钥路径；TLS 握手失败与证书校验失败分别给出可操作的错误提示。

### 体验优化

- 从应用菜单手动检查更新且没有新版本时，弹窗提示“当前已是最新版本”；检查失败时也会显示明确结果。
- 连接列表明确标识 MySQL / PostgreSQL，PostgreSQL 按“数据库 → Schema → 表”层级展示，避免混淆两种数据库的命名空间语义。
- Schema 元数据使用按连接和命名空间隔离的内存 LRU cache；支持手动刷新，并在重连、建库或执行 DDL 后自动失效。
- 快速来回切换 database、Schema 或表列时，过期异步响应不再回写当前对象树或 SQL 补全数据。
- SSH 隧道断开后可从顶部、断链提示或连接右键菜单一键重连；重连会先取消该连接的旧查询并清理旧连接池和隧道，迟到结果或旧 session 事件不会污染新会话。
- 测试连接支持带口令的 SSH 私钥；输入的 passphrase 只用于本次握手，不会保存。
- SSH 隧道运行中断开时会区分首跳掉线、嵌套通道关闭和内部监听异常，并避免正常关闭或重复事件造成误报。
- SSH keepalive 间隔与连续失败阈值可在高级设置中配置，新建连接默认启用 `60 秒 / 3 次`；关闭后不会再发送心跳。
- PostgreSQL 浏览非当前数据库时，错误横幅提供「一键切换」按钮：以会话级覆盖重建连接并展开该库的 Schema，不修改保存的连接配置，关闭后重新打开仍是原数据库。
- MySQL 语法错误可安全标出服务端行号；应用只接收错误 key 与行号，不会暴露原始数据库错误或 SQL 片段。
- 查询结果表格的 `#` 序号列冻结在左侧，横向滚动宽结果集时行号始终可见；表头继续吸顶，行列定位不迷失。

### 已知限制

- PostgreSQL 同一连接只能展开当前数据库；浏览其他数据库可通过错误横幅一键切换（仅本次会话生效，不改保存的连接配置）。证书路径配置尚未接入 PostgreSQL，当前使用驱动默认 TLS 策略。
- MySQL TLS 的错误提示已细化，真实 TLS 服务器（含双向证书）正反例已验收通过。
- SQL 历史在启用主密码但未解锁时不可读写；未启用主密码时由本机密钥加密保护。
- 导出 Excel 大结果集受查询 10 万行硬上限约束；超出请补充 `LIMIT` 分批导出。

---

## [0.1.0] — 2026-08-18

### 新功能

- SQL 编辑器支持执行 `SHOW` / `EXPLAIN` / `DESC` 等元数据语句并展示结果集，不再误判为写操作。

### 修复

- 修复点击 / 双击同一张表会开出多个同名预览 tab 的问题；已打开该表时点击会切换到已有 tab。
- 修复 `SELECT *` 多表 JOIN 因重复列名报错（1060 Duplicate column name）的问题；带 `LIMIT`、`FOR UPDATE` 等子句的查询不再被改写。
- 超出行数上限截断结果时主动终止服务端查询，避免大结果集在多跳隧道上继续传输。
- 修复并发查询占满连接池时相互等待导致整体卡死的问题。

### 安全与稳定性

- 「测试连接」现在同样校验 SSH 主机指纹（首次信任确认、指纹变更硬拒绝），不再接受任意主机密钥。

### 已知限制

- 带口令的 SSH 私钥暂不能通过「测试连接」输入 passphrase；保存连接后通过正式连接流程输入即可。
- SSH 运行期断开统一显示为对应跳板 `lost`，暂不细分 channel 主动关闭与本地 accept loop 异常。
- MySQL 服务端 SQL 错误暂不定位到编辑器具体行号，当前只显示稳定的中文错误提示。

---

## [0.0.3] — 2026-07-03

### 体验优化

- SQL 编辑区接入语法高亮、行号、错误标识和快捷执行，输入与排错体验更接近桌面数据库客户端。
- 数据库 / 表树新增图标并去掉数据库名前的展开箭头，提升多库、多表浏览时的识别度。

### 修复

- 修复数据库展开 / 收起交互：双击数据库才打开或切换，单击仅折叠 / 展开已打开的数据库；收起时保留当前表和查询结果，只有切换数据库时才重置表选择。

---

## [0.0.2] — 2026-07-01

### 新功能

- 已连接的连接右键菜单新增「新建数据库」，支持选择字符集、排序规则并预览 SQL。

### 体验优化

- 新建 / 编辑连接弹窗改为标签页布局，按常规、SSH、SSL 和高级设置分组。
- SSL 模式与证书路径可随连接保存；高级设置新增保持连接、连接 / 读取 / 写入超时、压缩和自动连接选项。
- 检查更新入口移到 macOS 应用菜单，连接列表头部不再放置更新按钮。

---

## [0.0.1] — 2026-06-30

### 新功能

- 初始桌面客户端能力：支持创建、编辑、复制、删除、测试并打开 MySQL 连接。
- 支持通过多跳 SSH 跳板连接内网 MySQL，并展示连接过程和断开状态。
- 支持首次连接新 SSH 主机时确认指纹，后续连接自动复用已信任记录。
- 支持浏览数据库、表、字段和表数据，并在内置 SQL 编辑器中执行查询。
- 支持取消长时间运行的 SQL，并在执行写操作前进行二次确认。
- 支持连接配置加密保存，避免敏感连接信息明文落盘。
- 支持应用内检查正式版更新、查看版本说明、下载更新并在安装后重启。

### 体验优化

- 连接列表改为更接近桌面客户端的操作方式：单击选中、双击连接、右键菜单管理连接。
- 新建 / 编辑连接、删除确认、SQL 写操作确认统一改为弹窗交互。
- 优化数据结果表格，较大结果集滚动更顺滑，并在结果被截断时给出提示。
- 优化连接拓扑展示，清楚呈现本机、跳板机和 MySQL 的连接路径与状态。
- 更新 tiny-sql 专属应用图标和左上角品牌标识。

### 安全与稳定性

- 增加 SQL 执行护栏，降低误执行多语句、空语句或危险写操作的风险。
- 增加 SSH keepalive 和连接丢失提示，方便定位是哪一跳出现问题。
- 指纹变更会被拒绝，避免连接到不可信的 SSH 主机。
- MySQL 连接默认更适配内网环境，减少因服务端 TLS 配置不一致导致的连接失败。

### 发布准备

- 完善 macOS、Windows、Linux 的桌面应用打包流程。
- 完善 GitHub Release 发布说明、预发布标记和正式版自动更新源生成。
- 修复正式版自动更新清单生成异常，确保 Linux 版本也能进入更新源。
- 补齐发布检查清单和 dogfooding 记录模板，为 v0.1 正式版试用做准备。

### 待验证

- 真实 3 跳 SSH + MySQL dogfooding 仍待完成。
- MySQL 5.7 兼容性仍待真实环境验证。
- 自动更新端到端流程仍需通过真实发布版本验证。
