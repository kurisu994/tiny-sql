# tiny-sql workspace 根
# 三个成员：ssh-multihop（隧道）/ db-driver（三数据库 Driver）/ src-tauri（壳）
[workspace]
resolver = "2"
members = ["crates/ssh-multihop", "crates/db-driver", "src-tauri"]

[workspace.package]
edition = "2021"
rust-version = "1.77.2"
license = "MIT"
authors = ["kurisu <makise_kurisuu@outlook.jp>"]
repository = "https://github.com/kurisu994/tiny-sql"

# 公共依赖在此统一版本，各 crate 用 { workspace = true } 引用
[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
tokio-util = "0.7"
futures-util = "0.3"
thiserror = "2"
log = "0.4"
serde = { version = "1", features = ["derive"] }
russh = "0.54"
sqlx = { version = "0.8", default-features = false, features = [
    "mysql",
    "postgres",
    "sqlite",
    "runtime-tokio-rustls",
    "chrono",
    "bigdecimal",
    "json",
] }
